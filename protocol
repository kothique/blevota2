client:
  CONTROLS:
    left: boolean
    right: boolean
    up: boolean
    down: boolean

    lmb: boolean
    rmb: boolean
    lmb: boolean
    mx: number
    my: number

server:
  WORLD:
    world:
      data: [{ x, y, r }]