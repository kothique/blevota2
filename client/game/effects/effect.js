/**
 * @module client/game/effect
 */

class Effect {
  /**
   * Read effect info except for type from a buffer.
   *
   * @param {Buffer} buffer
   * @param {number} offset
   * @return {number} - New offset.
   */
  parse(buffer, offset = 0) {
    return offset
  }
}

export default Effect